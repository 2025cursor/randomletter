# Example environment variables
# Copy this file to .env.local for local development
# Set these in Vercel dashboard for production

# Base URL for your application (HIGHEST PRIORITY)
# Set this in Vercel Environment Variables for your custom domain
NEXT_PUBLIC_BASE_URL=https://randomletter.cc

# Priority order for domain selection:
# 1. NEXT_PUBLIC_BASE_URL (your custom domain - highest priority)
# 2. VERCEL_URL (auto-set by Vercel)
# 3. http://localhost:3000 (local development fallback)